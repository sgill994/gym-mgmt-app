import React, {useState} from 'react';

const CalendarSchedule = ({view, classes}) => {
    const [selectedDay, setSelectedDay] = useState(new Date().getDay());

    return (
        <div>

        </div>
    );
};

export default CalendarSchedule;